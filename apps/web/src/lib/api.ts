const API=import.meta.env.VITE_API_URL||'http://localhost:4000'; async function req(path:string,init?:RequestInit){const r=await fetch(API+path,{headers:{'content-type':'application/json'},...init}); if(!r.ok)throw new Error(await r.text()); return r.json();} export const getMarkets=()=>req('/markets'); export const getMarket=(id:string)=>req('/markets/'+id); export const previewTrade=(body:any)=>req('/trades/preview',{method:'POST',body:JSON.stringify(body)}); export const executeTrade=(body:any)=>req('/trades/execute',{method:'POST',body:JSON.stringify(body)}); export const explain=(body:any)=>req('/explain',{method:'POST',body:JSON.stringify(body)}); export const getTrades=()=>req('/trades');
