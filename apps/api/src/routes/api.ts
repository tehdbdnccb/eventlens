import type {FastifyInstance} from 'fastify'; import {detail,execute,preview,snapshot,trades} from '../services/marketService.js';
export async function apiRoutes(app:FastifyInstance){app.get('/health',async()=>({ok:true,service:'eventlens-api',mode:process.env.DEMO_MODE!=='false'?'demo':'live'}));app.get('/markets',async()=>snapshot());app.get('/markets/:id',async(req:any)=>detail(req.params.id));app.get('/positions',async()=>trades());app.get('/trades',async()=>trades());app.post('/trades/preview',async(req:any)=>preview(req.body));app.post('/trades/execute',async(req:any)=>execute(req.body));app.post('/explain',async(req:any)=>({text:`EventLens sees a ${(Math.abs(req.body.edge||0)*100).toFixed(1)} percentage-point dislocation. The explanation is derived from the deterministic signal inputs; AI is intentionally not allowed to override execution or risk gates.`}));}
